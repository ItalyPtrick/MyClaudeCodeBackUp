# 全局规范

## 环境

- OS: Windows 11, PowerShell 终端
- Python: 3.12.10, Miniconda 包管理
- 编辑器: VS Code
- 路径分隔符：优先正斜杠 /，Windows 路径用双反斜杠 \\

## 主要开发方向

AI 应用开发：FastAPI 后端 + Streamlit 前端 + 大模型 API 调用 + RAG

---

## 会话启动协议

**每次新会话开始，必须按顺序执行以下步骤，完成后再响应任务：**

1. 读取本文件（CLAUDE.md）确认规范
2. 读取 `progress.txt`，了解当前进度、未完成任务、已知 Bug
3. 读取 `lessons.md`，回顾历史教训，本次会话中主动避免
4. 用一句话向我确认："已同步进度：[当前状态摘要]，准备开始。"

> 如果 progress.txt 或 lessons.md 不存在，告知我并建议创建。

---

## 代码规范

### 语言与框架

- Python 3.12，禁止使用 Python 2 语法
- 后端使用 FastAPI，禁止使用 Flask 或 Django（除非明确要求）
- 前端 Demo 使用 Streamlit
- 函数最大 50 行，超出则拆分
- 禁止裸 except，必须捕获具体异常类型

### 架构

- 架构分层：API 路由层 → 服务层（Service）→ 数据层（Repository）
- 每个 FastAPI 路由必须有对应的 Pydantic 请求/响应模型
- 大模型调用统一封装在 Service 层，禁止在路由层直接调用
- 使用 async/await，禁止在异步函数中使用同步阻塞调用
- 配置统一读取自 .env 文件（python-dotenv），禁止硬编码

### 类型与质量

- 所有函数必须有类型注解（Type Hints）
- 使用 Pydantic 做数据校验，禁止裸 dict 传递结构化数据
- 代码格式化使用 Black，import 排序使用 isort

### 依赖管理

- 使用 conda 管理虚拟环境，每个项目独立环境
- 依赖版本固定在 requirements.txt，格式：`package==x.x.x`
- 不得手动修改 requirements.txt 中的版本号，除非明确要求

---

## 工作流规范

- 新功能模块一律先 /plan，Bug 修复视复杂度决定
- 每次编码完成后提示运行类型检查和 Lint
- Git 提交使用 Conventional Commits 格式（feat/fix/refactor/chore/docs/test）
- 不得修改 requirements.txt 依赖版本，除非明确要求
- 提交前执行 /code-review

### Plan 模式行为规范

进入 /plan 后，**先向我提问，再制定计划**：

- 每轮最多提 3 个问题，等待我回答后再继续
- 直到以下维度都已明确，才开始输出计划：
  - 功能边界（做什么、明确不做什么）
  - 涉及的层和模块（路由 / Service / 数据层 哪些需要改动）
  - 异常与边界条件（API 调用失败、空数据、超时等）
- 如果我的描述已足够清晰，可以跳过提问直接出计划，但需注明"需求已明确，直接制定计划"

---

## progress.txt 维护规范

`progress.txt` 是会话间的记忆桥梁，**每次完成一个功能或结束会话前必须更新**。

### 格式模板

```
== 最后更新 ==
[日期 / 功能名称]

== 已完成 ==
- [功能名]：[一句话描述完成状态]

== 进行中 ==
- [任务名]：[当前卡在哪一步]

== 接下来 ==
- [下一个任务]

== 已知 Bug ==
- [Bug 描述]（[发现时间 / 触发条件]）
```

### 更新规则

- 完成任何一个功能模块后立即更新
- /clear 或结束会话前必须更新
- 不得只更新代码不更新 progress.txt

---

## Python 常用命令

```powershell
# 环境管理
conda create -n [项目名] python=3.12   # 创建环境
conda activate [项目名]                # 激活环境
conda deactivate                       # 退出环境

# 依赖管理
pip install -r requirements.txt        # 安装依赖
pip freeze > requirements.txt         # 导出依赖

# 启动服务
uvicorn main:app --reload              # 启动 FastAPI（开发模式）
streamlit run app.py                   # 启动 Streamlit

# 代码质量
black .                                # 格式化代码
isort .                                # 排序 import
```

---

## 测试规范

- 遵循 TDD：先写测试（RED），再实现（GREEN），再重构（IMPROVE）
- 核心业务逻辑覆盖率目标 ≥ 80%
- 测试框架使用 pytest
- 异步测试使用 pytest-asyncio
- 大模型 API 调用必须 mock，禁止在测试中真实调用外部 API

```powershell
pytest                        # 运行所有测试
pytest tests/ -v              # 详细输出
pytest --cov=. --cov-report=html  # 覆盖率报告
```

---

## 文档查询规则

涉及 FastAPI、LangChain、ChromaDB、Pydantic、Streamlit API 时：

- 使用 context7 插件查询最新官方文档（/context7 关键词）
- 避免使用废弃 API
- 不得猜测库的版本，必须查 requirements.txt

---

## 安全规范

- 不得将任何 API key、token、密码硬编码进源文件
- 所有密钥统一存放在 .env 文件，通过 python-dotenv 读取
- .env 文件必须加入 .gitignore，禁止提交到 Git
- 涉及用户数据、外部 API 调用时，主动提示安全风险
- 不在来路不明的仓库目录下启动 Claude Code

---

## 已安装插件与用途

| 插件                   | 主要命令                                     | 用途                      |
| ---------------------- | -------------------------------------------- | ------------------------- |
| context7               | /context7 关键词                             | 查最新官方文档            |
| code-review            | /code-review                                 | 多代理并行代码审查        |
| feature-dev            | /feature-dev "描述"                          | 7阶段结构化功能开发       |
| commit-commands        | /commit /commit-push-pr                      | 规范提交、一键创建PR      |
| github                 | 自然语言调用                                 | 仓库/PR 管理（需 gh CLI） |
| serena                 | 自然语言调用                                 | 语义代码分析，跨文件导航  |
| superpowers            | 自动生效                                     | 子代理增强                |
| everything-claude-code | /plan /tdd /build-fix /refactor-clean /agent | 完整开发工作流            |

---

## 典型工作流

### 新功能开发

```
/plan "描述需求"
  ↓ AI 提问澄清（每轮≤3问），我回答
  ↓ 确认计划
/agent "按计划实现"
  ↓ 实现完成
/code-review
  ↓ 修复问题
/commit
  ↓ 更新 progress.txt
```

### Bug 修复

```
直接描述问题
  ↓
/code-review
  ↓
/commit
  ↓ 更新 progress.txt（含 Bug 已修记录）
```

### 查文档

```
/context7 FastAPI dependency injection
/context7 LangChain RAG retrieval chain
/context7 ChromaDB collection query
```

### TDD 开发

```
/tdd 实现 DocumentService.query()，需要处理：向量库为空、query 超长、embedding 调用失败
```

---

## 日常操作

### 启动

永远在项目目录下启动，不要在 home 目录启动：

```powershell
cd C:\你的Python项目路径
conda activate [项目名]
claude
```

### 上下文管理

```
/clear    # 切换新任务时清空上下文（清空前先更新 progress.txt）
/compact  # 上下文过长时压缩（保留摘要）
```

### 常用快捷键

| 快捷键            | 作用                           |
| ----------------- | ------------------------------ |
| Shift+Tab（两次） | 切换 Plan Mode（只分析不动手） |
| Esc               | 打断当前执行                   |
| Ctrl+C            | 强制停止                       |

---

## 禁止操作

- 不得猜测依赖版本，必须查 requirements.txt
- 不得在路由层直接调用大模型 API，必须通过 Service 层
- 不得生成没有被要求的功能
- 不得将 .env 文件提交到 Git
- 不得直接推送到 main 分支
- 不得修改 .gitignore（除非明确要求）

---

## 自我改进协议

每次我纠正你的错误后，执行以下操作：

1. 将该错误追加到 `lessons.md`，格式如下：
   ```
   ### [日期] [错误简述]
   **错误模式：** [什么情况下触发了什么错误]
   **正确做法：** [应该怎么做]
   ```
2. 如果该规则具有普遍性，同步更新本文件对应章节
3. 回复我："已记录到 lessons.md，下次不会重复。"

> lessons.md 在每次会话启动时读取，是错误不重复的唯一保障。

### lessons.md 初始内容

首次使用时创建 `lessons.md`，写入以下内容：

```markdown

### 环境未激活问题

**错误模式：** 未激活 conda 环境直接运行代码
**后果：** 使用了全局 Python，依赖缺失或版本冲突
**正确做法：** 启动前确认 conda activate [项目名]，PowerShell 提示符会显示环境名

### .env 文件泄露风险

**错误模式：** .env 文件未加入 .gitignore 就执行了 git add .
**后果：** API key 提交到远程仓库，存在安全风险
**正确做法：** 新建项目第一步就配置 .gitignore，包含 .env 和 __pycache__
```
