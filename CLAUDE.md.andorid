# 全局规范

## 环境

- OS: Windows 11, PowerShell 终端
- Node: v24, npm v11
- 路径分隔符：优先正斜杠 /，Windows 路径用双反斜杠 \\

## 主要开发方向

Android 原生开发，使用 Android Studio + Kotlin + Jetpack Compose

---

## 会话启动协议

**每次新会话开始，必须按顺序执行以下步骤，完成后再响应任务：**

1. 读取本文件（CLAUDE.md）确认规范
2. 读取 `progress.txt`，了解当前进度、未完成任务、已知 Bug
3. 读取 `lessons.md`，回顾历史教训，本次会话中主动避免
4. 用一句话向我确认："已同步进度：[当前状态摘要]，准备开始。"

> 如果 progress.txt 或 lessons.md 不存在，告知我并建议创建。

---

## 代码规范

### 语言与 UI

- Kotlin 优先，禁止新增 Java 文件
- 使用 Jetpack Compose，禁止新增 XML 布局
- 函数最大 50 行，超出则拆分
- 禁止使用 !! 操作符（生产代码）

### 架构

- 架构：MVVM + Clean Architecture（UI → Domain → Data）
- 每个 ViewModel 必须有对应 UiState 数据类
- API 响应统一用 Result<T> 包装
- 使用 sealed class/interface 表达状态机和结果类型
- 禁止绕过 Repository 层直接在 ViewModel 操作数据源

### 响应式与 DI

- 响应式：Kotlin Flow + StateFlow，禁止新增 LiveData
- DI 框架：Hilt

### 协程规范

- 禁止在生产代码中使用 Thread.sleep 或 runBlocking
- ViewModel 用 viewModelScope，其他用 CoroutineScope(SupervisorJob())
- 长循环中检查 isActive，超时用 withTimeout

---

## 工作流规范

- 新功能模块一律先 /plan，Bug 修复视复杂度决定
- 每次编码完成后提示运行类型检查和 Lint
- Git 提交使用 Conventional Commits 格式（feat/fix/refactor/chore/docs/test）
- 不得修改 build.gradle 依赖版本，除非明确要求
- 提交前执行 /code-review

### Plan 模式行为规范

进入 /plan 后，**先向我提问，再制定计划**：

- 每轮最多提 3 个问题，等待我回答后再继续
- 直到以下维度都已明确，才开始输出计划：
  - 功能边界（做什么、明确不做什么）
  - 涉及的层和模块（UI / Domain / Data 哪些需要改动）
  - 异常与边界条件（网络失败、空数据、并发等）
- 如果我的描述已足够清晰，可以跳过提问直接出计划，但需注明"需求已明确，直接制定计划"

---

## progress.txt 维护规范

`progress.txt` 是会话间的记忆桥梁，**每次完成一个功能或结束会话前必须更新**。

### 格式模板

```
== 最后更新 ==
[日期 / 功能名称]

== 已完成 ==
- [功能名]：[一句话描述完成状态]

== 进行中 ==
- [任务名]：[当前卡在哪一步]

== 接下来 ==
- [下一个任务]

== 已知 Bug ==
- [Bug 描述]（[发现时间 / 触发条件]）
```

### 更新规则

- 完成任何一个功能模块后立即更新
- /clear 或结束会话前必须更新
- 不得只更新代码不更新 progress.txt

---

## Android 构建命令

- 构建: ./gradlew assembleDebug
- 单元测试: ./gradlew testDebugUnitTest
- Lint: ./gradlew lint
- 清理: ./gradlew clean

---

## 测试规范

- 遵循 TDD：先写测试（RED），再实现（GREEN），再重构（IMPROVE）
- 核心业务逻辑覆盖率目标 ≥ 80%
- Flow 测试使用 Turbine
- 协程测试使用 runTest（kotlinx-coroutines-test）
- 新功能模块必须有对应单元测试

---

## 文档查询规则

涉及 Compose、Coroutines、Room、Hilt、Retrofit API 时：

- 使用 context7 插件查询最新官方文档（/context7 关键词）
- 避免使用废弃 API
- 不得猜测依赖版本，必须查 libs.versions.toml 或 build.gradle

---

## 安全规范

- 不得将任何 API key、token、密码硬编码进源文件
- 敏感配置统一读取自 local.properties 或环境变量
- 不得读取或输出 key.properties 的内容
- 涉及网络请求、权限、用户数据存储时，主动提示安全风险
- 不在来路不明的仓库目录下启动 Claude Code

---

## 已安装插件与用途

| 插件                   | 主要命令                                     | 用途                      |
| ---------------------- | -------------------------------------------- | ------------------------- |
| context7               | /context7 关键词                             | 查最新官方文档            |
| code-review            | /code-review                                 | 多代理并行代码审查        |
| feature-dev            | /feature-dev "描述"                          | 7阶段结构化功能开发       |
| commit-commands        | /commit /commit-push-pr                      | 规范提交、一键创建PR      |
| github                 | 自然语言调用                                 | 仓库/PR 管理（需 gh CLI） |
| serena                 | 自然语言调用                                 | 语义代码分析，跨文件导航  |
| superpowers            | 自动生效                                     | 子代理增强                |
| kotlin-lsp             | 自动生效                                     | Kotlin 语言服务器         |
| everything-claude-code | /plan /tdd /build-fix /refactor-clean /agent | 完整开发工作流            |

---

## 典型工作流

### 新功能开发

```
/plan "描述需求"
  ↓ AI 提问澄清（每轮≤3问），我回答
  ↓ 确认计划
/agent "按计划实现"   ← 任务明确时用 agent，无需中途决策
  ↓ 实现完成
/code-review
  ↓ 修复问题
/commit
  ↓ 更新 progress.txt
```

### Bug 修复

```
直接描述问题
  ↓
/code-review
  ↓
/commit
  ↓ 更新 progress.txt（含 Bug 已修记录）
```

### 查文档

```
/context7 Jetpack Compose LazyColumn
/context7 Hilt ViewModel injection
```

### TDD 开发

```
/tdd 实现 CartRepository.addItem()，需要处理：库存不足、重复添加、网络错误
```

---

## 日常操作

### 启动

永远在项目目录下启动，不要在 home 目录启动：

```powershell
cd C:\你的Android项目路径
claude
```

### 上下文管理

```
/clear    # 切换新任务时清空上下文（清空前先更新 progress.txt）
/compact  # 上下文过长时压缩（保留摘要）
```

### 常用快捷键

| 快捷键            | 作用                           |
| ----------------- | ------------------------------ |
| Shift+Tab（两次） | 切换 Plan Mode（只分析不动手） |
| Esc               | 打断当前执行                   |
| Ctrl+C            | 强制停止                       |

---

## 禁止操作

- 不得猜测依赖版本，必须查 libs.versions.toml 或 build.gradle
- 不得绕过 Repository 层直接在 ViewModel 操作数据源
- 不得生成没有被要求的功能
- 不得修改 keystore、key.properties、google-services.json、local.properties
- 不得直接推送到 main 分支
- 不得修改 .gitignore（除非明确要求）

---

## 自我改进协议

每次我纠正你的错误后，执行以下操作：

1. 将该错误追加到 `lessons.md`，格式如下：
   ```
   ### [日期] [错误简述]
   **错误模式：** [什么情况下触发了什么错误]
   **正确做法：** [应该怎么做]
   ```
2. 如果该规则具有普遍性，同步更新本文件对应章节
3. 回复我："已记录到 lessons.md，下次不会重复。"

> lessons.md 在每次会话启动时读取，是错误不重复的唯一保障。

### lessons.md 初始内容（迁移自原已知坑）

首次使用时创建 `lessons.md`，写入以下内容：

```markdown
### 启动目录问题

**错误模式：** 在 C:\Users\admin 目录下启动 Claude Code
**后果：** .claude 目录被误识别为项目，settings.json 被加载两次
**正确做法：** 永远在项目目录下启动

### .claude.json 配置冲突

**错误模式：** .claude.json 里存在 "C:/Users/admin/.claude" 这条 projects 记录
**后果：** 配置冲突，导致所有消息 Interrupted
**正确做法：** 发现此记录立即删除
```
